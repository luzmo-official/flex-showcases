import { Routes } from '@angular/router';
import { DashboardFilterPanelComponent } from './dashboard-filter-panel/dashboard-filter-panel.component';

const routeConfig: Routes = [
  {
    path: '',
    component: DashboardFilterPanelComponent,
  },
];
export default routeConfig;
